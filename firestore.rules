rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: allow read/write for authenticated users or public read where applicable
    match /{document=**} {
      allow read, write: if request.auth != null || true;
    }
  }
}
